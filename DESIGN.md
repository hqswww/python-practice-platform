# 编程练习册 — 项目设计文档

> 目标：面向计算机入门后辈的**编程**练习/检测综合平台（Python / C / C++，可继续扩语言）
> 形态：Flutter 本地桌面应用（Windows / macOS / Linux）
> 状态：**v1.3.0**（三门语言各 72 题 + 首次运行向导 + 安装包分发）
>
> 📌 阅读提示：本文档**保留了两段历史记录**（v1.0 小结、里程碑），
> 里面的「72 道题」「5 项测试」等数字是当时的真实状态，不是笔误。
> 想知道**现在**是什么样，看 README 的版本段与 `PROJECT_BACKLOG.md`。
> 语言维度与题库规格详见 `PROJECT_MAP.md`、`tools/BANK_SPEC.md`。

## 一、核心闭环
学生选题 → 写代码 → 运行（Python 解释执行 / C·C++ 先编译再跑）→ 比对输出 → 对/错反馈 + 进度记录

## 二、技术决策（已拍板）
| 项 | 决策 |
|----|------|
| 框架 | Flutter（Dart），开发者已有 Flutter Android 经验 |
| 语言范围 | **Python / C / C++ 三个独立科目**，不是合并的「C/C++」 |
| 开发机 | macOS（`~/Develop/flutter`；项目最初在 Linux 上起头） |
| 分发目标 | **Windows + macOS + Linux**；出安装包（Windows Setup.exe / macOS dmg） |
| 判题 | **按语言分两条路**：Python 捆绑解释器解释执行；C/C++ 用系统编译器先编译 |
| C/C++ 编译器 | **用系统编译器**，不捆进应用（clang + SDK 几百 MB，且许可证不允许再分发） |
| 编辑器 | 自研 `PythonCodeField`：语法高亮 + 行号，零第三方依赖 |
| 判题交互 | 即时运行 + 自动标对错；运行面板可逐步喂输入 |
| 进度存储 | shared_preferences（本地持久化），**键带语言** |
| 防作弊 | 不做（自习自测场景） |
| 结果显示 | 可切换：简洁模式(对/错) / 详细模式(实际vs期望 + traceback) |

## 三、题库结构（对齐 runoob 学习进度）

**按语言分目录**，每门语言 12 个分类、72 道题：

```
assets/problems/
├── python/   01_syntax … 12_challenges    （基础语法 → 综合挑战）
├── c/        01_basics … 12_challenges    （基础语法 → 指针 → 结构体 → 综合挑战）
└── cpp/      01_basics … 12_challenges    （基础语法 → 类 → 继承 → STL → 综合挑战）
```

**id 段按分类序号走**（`NN*100+1..6`），三门语言各自独立 ——
进度键是「语言_题号」，撞号会让两道题共享进度。

⚠️ 加语言时三个落点缺一不可：新建目录 + `pubspec.yaml` 登记（**资源声明不递归**）
+ `problem_repository.dart` 的分类表。漏了分类表那门语言会少一块，且**不会报错**。

难度：easy / medium / hard（3 级）
- 入门同学接受能力偏弱 → 保留细分分类，循序渐进
- 高难度题描述可更详细

## 四、题目 JSON 格式 (v1)
```json
{
  "id": 101,
  "title": "Hello, World!",
  "difficulty": "easy",
  "description": "...",
  "input_format": "...",
  "output_format": "...",
  "sample_input": "...",
  "sample_output": "...",
  "test_cases": [
    {"input": "...", "output": "..."}
  ],
  "source_requirements": [              // 可选，见第五节
    {"check": "pointer.use", "label": "给学生看的要求", "hint": "怎么改"}
  ],
  "hints": ["提示1", "提示2"]   // 多级提示，逐步揭示
}
```

## 五、判题反馈机制（友好错误提示层）⭐ 用户体验灵魂
判题失败时，在简单显示"错误"前，先做智能检测：
| 常见错误 | 检测 | 提示 |
|---------|------|------|
| 空格/换行问题 | 去空白后一致但原样不同 | 内容对但多了/少了空格或换行，注意格式 |
| 多余输出 | 实际输出含预期输出 | 结果包含在输出中，可能多了额外内容 |
| 中文标点/全角 | 输出含全角字符 | 检查是否用了中文标点（应为英文半角） |
| 没读入输入 | 输出固定与输入无关 | 可能需要用 input() 读入输入 |
| **输出全对但没用上要求的语法** | 源码里查（见下） | 说清「输出没错，差的是语法」+ 怎么改 |

核心：**先做"空白规范化比对"**，判定后给针对性提示，而非干巴巴"答案错误"。

### 五·补、源码语法要求检查（v1.4.0）

**问题**：判题只比对 stdout，而有些题目的输出**用不用那个语法完全一样**。
真实案例 —— 题库 901「用指针读取变量的值」期望输出是同一个数打两遍，于是这段
一个指针都没有的代码照样判过（学生实际报上来的）：

```c
int a;  scanf("%d", &a);
int b = a;                      /* 普通拷贝 */
printf("%d\n%d", a, b);         /* 与 *p 版本一字不差 */
```

这不是引擎 bug，是「只比对输出」这种判题方式的固有边界：输出层面无法分辨，
就只能到源码层面补一道检查。

**做法**（`lib/services/source_check.dart`）：

1. 题库为这类题声明 `source_requirements`（检查名 + label + hint）
2. 判题在**输出全对**之后，把**去掉注释和字符串字面量**的源码交给具名检查
3. 有没满足的 → `JudgeResult.unmetRequirements` 非空 →
   `allPassed` 为 false → 进度不记、下一题按钮不出现，面板显示橙色「输出对了，
   但没按要求用上语法」块

关键取舍：

- **输出不对时完全不查**。真实的错优先，同时甩两条互不相干的结论只会让学生
  不知道该先改哪个。
- **必须先去注释和字面量**。学生在注释里留着 `// int *p = &n;` 是极常见的事，
  直接匹配原文会把它算成「用过指针」——而那行根本没在运行。
- **是启发式，不是语义分析**。挡得住「压根没用指针」「声明了却不用」
  「只取一个元素就改用数组下标」，挡不住蓄意伪装。所以：
  - 参考答案必须在题库自检测试里**满足自己声明的每一条要求**
    （照抄参考答案都判不过是最坏的情况）
  - 设置页留了开关（`判题 → 源码语法要求检查`，默认开），
    万一误判学生还有路可走
- **`*` 有三种含义**，`int *p`（声明）、`a * p`（乘法）、`*p`（解引用）
  靠「星号前面那个字符」区分；`return *p;` 要特判 —— 不特判会把它当乘法漏掉。


## 六、里程碑
- [x] 需求/设计讨论定稿
- [x] 题目格式 + 题库结构 + 判题反馈机制设计
- [x] 安装 Flutter SDK（~/flutter 3.44.9，PATH 已写入 fish）
- [x] 创建项目骨架（flutter create --platforms=linux）
- [x] 实现题目数据模型 + 题库加载
- [x] 实现判题引擎核心（judge_engine.dart，5 项测试通过）
- [x] 实现题目列表页 + 编辑器判题页（左右分栏）
- [x] 实现判题结果展示（简洁/详细模式）
- [x] MD3 主题 + 深色模式 + “下一题”按钮
- [x] 题库 12 分类全部填充完成（72 道题）
- [x] 底部 NavigationBar 三板块（练习/测试/设置）
- [x] 进度存储（shared_preferences，做对自动标记 + 练习页进度圆环/勾选）
- [x] 测试板块（随机抽题组卷、**自由选题跳题**、逐题判题、交卷汇总得分）
- [x] 设置板块（主题模式切换、判题超时滑块、真实难度统计详情、清除进度）
- [x] UI 动画体系（设置卡片 hover、判题结果交错入场、切题滑动、结果庆祝、练习网格入场、导航切换过渡）
- [x] 迁移 Windows 构建 .exe
- [x] 多语言重构：语言模型 / 进度主键带语言 + 旧数据迁移 / 运行时抽象 / 语法高亮按语言 / 顶栏切换器
- [x] **接入 C**（12 分类 72 题）+ **接入 C++**（12 分类 72 题），各带教程
- [x] 运行面板按语言分「交互终端 / 编译运行」，都能逐步喂输入
- [x] **首次运行向导**：运行环境自检 + 一键装编译器（Windows）
- [x] **安装包分发**：Windows `Setup.exe`（免管理员）、macOS `dmg`（拖拽安装）
- [x] macOS 迁移 + 签名 + 14 项自动验收
- [x] Linux 打包（绿色版 tar.gz + 用户级 `install.sh`）

## 七、后端引擎已验证的技术点
- `Process.start(python3, [solution.py])` + stdin/stdout 管道判题可行
- 判题通过判定用 `_normalizeLines`（去每行尾空白+统一换行），保留行内空格避免把"多打空格"误判为通过
- 格式错误走 `_analyzeWrongAnswer` 友好提示（去空白一致→提示格式；首个不同字符定位）
- 编译型语言：**编译一次、所有用例复用产物**；编译超时与运行超时**分开**
  （编译默认 10s，运行沿用设置里的判题超时）
- 超时后**必须真的杀掉进程**（只返回结果不杀的话，学生的 `while(1)` 会在后台一直跑）
- 测试：`flutter test` 全过；C/C++ 题库另有「参考答案必须全部判过」的自检

## 八、题库档案（3 × 72 = 216 道）
每门语言 12 分类 × 6 题，各分类均匀分布 easy/medium/hard 三级。

每题除测试用例外**必带教程与提示**（平台的定位是「边学边练」，缺教程会被结构守卫拦下）。

出题禁区（判题环境里做不到的事）：文件读写、打印内存地址、时间/随机数、联网。
详见 `tools/BANK_SPEC.md`。

## 九、UI 动画体系 ⭐（Material 3 动效）
全部基于 Flutter 内置动画组件，无第三方包：

| 位置 | 动画 | 实现 |
|------|------|------|
| 设置卡片 | hover 上浮 + 阴影加深 | `AnimatedScale` + `MouseRegion`（onEnter/onExit） |
| 设置进度详情 | 平滑展开/收起 | `AnimatedSize` + 箭头 `AnimatedRotation` |
| 判题结果面板 | 交错滑入 | `TweenAnimationBuilder(0→1)` + 每元素按 index 延迟（`_stagger`） |
| 判题结果切换 | 整体淡入缩放 | `AnimatedSwitcher` + key 绑自增 `_judgeRun`，Fade+Scale 组合 |
| 全部通过 | 头部卡片弹跳庆祝 | `TweenAnimationBuilder` + `Curves.elasticOut` |
| 测试切题 | 滑动+淡入 | `AnimatedSwitcher` + key 绑题目 id，Slide+Fade |
| 测试进度条 | 平滑填充 | `AnimatedFractionallySizedBox`（widthFactor 随进度） |
| 测试结果页 | 上移淡入 + 奖杯弹跳 | `TweenAnimationBuilder` + easeOutBack；得分≥80% 用 elasticOut |
| 练习分类网格 | 交错入场 | 整体 `TweenAnimationBuilder`，每卡片 Index×0.08 延迟的 `_stagger` |
| 分类卡片 | hover 缩放 | `AnimatedScale` + `MouseRegion`，StatefulWidget |
| 导航切换 | 轻微缩放+淡入 | `TweenAnimationBuilder` + key 绑 `_navTransition` |

### 交错入场通用手法（`_stagger`）
```dart
// 每元素延迟：start = index*间隔，end = start+窗口宽度
final start = (index * 0.08).clamp(0.0, 0.7);
final end = (start + 0.35).clamp(0.0, 1.0);
// 在单个 TweenAnimationBuilder(0→1) 里，把整体进度 t 映射到每个元素局部进度
// 再配 Curves.easeOutCubic 得到滑入感
```

### 关键设计原则
- **保留 IndexedStack 状态**：导航切换用 `TweenAnimationBuilder`（key 触发）而非 `AnimatedSwitcher`，避免重建导致测试会话/页面状态丢失。
- **判题结果每次触发**：用自增计数器 `_judgeRun` 做 key，即使结果相同也重新过渡。
- **交错用单控制器**：一个 `TweenAnimationBuilder(0→1)` 驱动所有元素，靠 index 计算延迟，避免多 controller 浪费。
- **切题 key 绑题目 id**：`ValueKey(problem.id)` 由 AnimatedSwitcher 识别切换。

### 值得注意的坑
- 交错 `_stagger` 计算要 clamp 到 [0,1]，避免 t 越界导致闪现。
- `AnimatedSwitcher` 换 key 会重建子树：不适合保状态场景，导航切换用 `TweenAnimationBuilder` 更合适。

## 十、v1.0 小结（第一版收官）✅
第一版基本功能已全部落地，本地可运行：

| 维度 | 内容 |
|------|------|
| 题库 | 12 分类 72 题（01 语法 → 12 综合挑战），对齐 runoob 学习进度，easy/medium/hard 三级 |
| 练习 | 分类网格 → 题目列表 → 代码编辑器；多级提示；实时判题 + 友好错误提示；做对自动存进度（shared_preferences） |
| 测试 | 随机抽题组卷（5/10/15/全部）；**自由选题跳题**（题号导航栏 + 每题独立草稿 + 判题反馈条 + 交卷汇总）；做对题数计分，≥80% 奖杯 |
| 设置 | 主题切换（系统/浅/深）、判题超时滑块、进度统计（按难度）、清除进度 |
| 动画 | 全套 MD3 动效（详见第九章） |
| 判题 | 捆绑 Python runtime，Process.start 喂输入比对输出；`_normalizeLines` 判定；友好错误检测层 |
| 验证 | `flutter analyze` 零问题、5 项测试全过、`flutter build linux --release` 成功、应用稳定运行 |

**技术栈**：Flutter 3.44.9 / Dart 3.12.2 / 无第三方动画包 / shared_preferences 持久化

## 十·补、v1.3.0 小结（多语言 + 安装包）✅

第一版只做 Python；到 v1.3.0 已经是三门语言、三平台、带安装包的分发形态。

| 维度 | 内容 |
|------|------|
| 语言 | **Python / C / C++ 三个独立科目**，各 12 分类 72 题、各带教程；进度/错题/收藏/成就按语言独立 |
| 判题 | 按语言分两条路：Python 解释执行（捆绑解释器）；C/C++ 系统编译器 + 编译一次复用产物 |
| 运行面板 | Python 是「交互终端」、C/C++ 是「编译运行」；**都能在程序读输入时逐步喂一行** |
| 上手 | **首次运行向导**：第一次打开就自检三门语言的运行环境，Windows 上可一键装编译器 |
| 诊断 | 环境故障与代码问题**分开报**；设置页显示「已就绪 + 实际路径」；日志中心含三门语言运行时 |
| 分发 | Windows `Setup.exe`（免管理员）+ 绿色版目录；macOS `dmg`（拖拽安装）+ zip；Linux tar.gz |
| 健壮 | Windows **中文用户名路径**防御（判题工作目录走 ASCII + 编译器 `-B`）；除零/崩溃/编译失败各有对症提示 |
| 验证 | `flutter test` 全过、`flutter analyze` 无新增、macOS 14 项自动验收；两个平台的安装包本身待实机 |

**技术栈（v1.3.0）**：Flutter 3.47.3 / 无第三方动画包 / shared_preferences 持久化 /
判题零网络依赖；C/C++ 走系统编译器（见下）

### 为什么 C/C++ 不捆绑编译器

Python 解释器 54MB/架构、可以整包带走；而 C 编译器在 macOS 上要 clang（98MB）
+ MacOSX.sdk（**258MB**，且 Apple 不允许再分发），universal 两份就是 +700MB。
所以 C/C++ 只能走「用系统编译器」路线 —— 与 Windows 装 MinGW-w64 是同一条思路。

代价是**用户机器上可能没有编译器**，所以补了两处提示：首次运行向导里的
「一键安装」（Windows）/ 下载页与安装命令（其它平台），以及设置页的运行时自检卡片。

## 十·补二、v1.4.0 小结（源码语法要求检查）✅

- 题库可为题目声明 `source_requirements`，判题在输出全对后额外核对源码
- 已覆盖 12 道题：C 指针 901~906、C++ 指针/引用 801~806
- 新增具名检查 `pointer.use` / `pointer.walk` / `reference.use`
- 新增 `LanguageRuntime.stripCommentsAndLiterals`（C 系 + Python）
- 结果面板新增橙色「输出对了，但没按要求用上语法」块
- 设置新增「源码语法要求检查」开关（默认开）
- 顺带修掉面板里写死的「正在运行 **Python** 判题…」（C/C++ 题目也显示 Python）
- **顺带修深浅主题下的可读性**（学生同时报上来的）：
  - 深色模式下判题结果的说明文字是黑的（面板里写死 `Colors.black54`，对比度 1.13）
  - 浅色模式下终端输入框的文字看不见 —— 根因是全局 `InputDecorationTheme`
    的 `filled: true` 在终端自己画的深色衬底上又刷了一层浅灰（对比度 1.00）。
    修的是输入框（`filled: false`）而不是把字改成黑的
  - 体检出 35 处硬编码灰（`Colors.grey` / `grey[...]`），在**某一个**主题下
    对比度低到 2.55~3.00，统一换成 `colorScheme.onSurfaceVariant`
  - 主题抽到 `lib/theme.dart`，`test/theme_contrast_test.dart` 用**界面真正
    在用的那份主题**做两套主题的可读性断言

### 主题与颜色的规矩

1. **不许按「看着差不多」挑灰色**。同一个灰在两套主题下可以完全相反：
   `Colors.grey` 在深色底 6.94、浅色底只有 2.55；`grey[700]` 反过来是 3.00 / 5.89。
   次级文字/图标一律 `colorScheme.onSurfaceVariant`（浅 8.87 / 深 10.97）。
2. **自己画了底色的控件必须显式 `filled: false`**，否则全局
   `InputDecorationTheme` 会再刷一层，把写死的文字色埋掉。
3. **`Colors.black.withValues(alpha: …)` 做衬底在深色主题下等于没画**。
   要「比背景稍微深一点/亮一点」就用 `onSurface.withValues(alpha: …)`。
4. **能不能看清靠算不靠看**：WCAG 对比度 `(亮+0.05)/(暗+0.05)`，
   `test/theme_contrast_test.dart` 里有现成实现。

**为什么不做「只写函数 + 判题注入 main」**：那是更彻底的做法（签名不对就编不过，
根本不用启发式），但要求把题面从「写一个完整程序」改成「只写一个函数」，
判题要拼接隐藏的测试驱动，题库 216 题的结构、教程、编辑器模板都要跟着动。
收益是 12 道题更严一点，代价是整套题面模型重构 —— 现阶段不划算，
这条记在 `PROJECT_BACKLOG.md` 里，将来真要上「严格判题」时再评估。

## 十·补三、v1.5.0 小结（自动检查更新）✅

启动时静默查一次 GitHub Releases（`api.github.com/repos/<repo>/releases/latest`），
有新版本就弹窗：写清「当前版本 → 最新版本」+ 更新内容 + 「去下载」。
设置页 `关于 → 检查更新` 里有开关（默认开）和「立即检查更新」。

### 为什么只跳转下载，不做自动安装

自动替换自己的可执行文件要在三个平台各写一套，且每套都要提权或绕过系统检查：

| 平台 | 难处 |
|------|------|
| macOS | 要往 `/Applications` 换 `.app`；未公证的包还会被 Gatekeeper 拦；改自己会破坏已签名 bundle 的完整性 |
| Windows | 正在运行的 `exe` 不能被覆盖，得先起一个分离进程、等自己退出后再替换 |
| Linux | 装在哪取决于用户怎么装的（系统包 / 解包目录），应用猜不出来 |

而「跳到下载页 → 用户双击安装包」三平台通用、不需要提权，且本项目的安装包
都是免管理员、装到用户目录的，**覆盖安装不会丢进度**（进度存在系统用户数据目录，
不在安装目录里）。这条承诺还依赖一个不变量：Windows 版的
`CompanyName` / `ProductName` 版本资源不能改 —— `shared_preferences` 的落盘路径
就是从它们拼出来的，改了老用户会「找不到自己的进度」。已有测试钉住。

### 三条必须守住的边界

1. **失败必须静默**。断网、公司代理、GitHub 不通、匿名 API 限流（60 次/小时/IP）
   都很常见，而它们跟「能不能做题」毫无关系。所以 `check()` 不抛异常，
   失败只返回 `failed`，启动那条路更是当没发生（只写一行日志）。
2. **仓库还没有 Release（HTTP 404）算「已是最新」**，不算错误 ——
   项目刚起步时 Releases 就是空的，报成「检查失败」会让用户以为网络坏了。
3. **同一个版本只提示一次**。用户点「跳过这个版本」会记下版本号；
   只跳过**这一个**，下个版本照常提示。设置页手动检查时不给这个按钮 ——
   用户是主动来问的，不该给他「以后别告诉我」。

### 几个实现细节

- **User-Agent 必须给**：GitHub API 对没有 UA 的请求直接 403
- **走系统代理**：`HttpClient.findProxyFromEnvironment`，国内直连 GitHub 常常不通
- **超时 5~6 秒**，且整个检查跑在首帧之后 —— 它绝不能拖慢启动
- **按扩展名挑安装包**（`.dmg` / `.exe` / `.tar.gz`），不按文件名：
  文件名里的「编程练习册」随时可能改，按名字匹配会变成「改个展示名就找不到包了」。
  挑不到就退回 Release 页面
- **打开链接零依赖**：`open`（macOS）/ `rundll32 url.dll,FileProtocolHandler`（Windows）/
  `xdg-open`（Linux），不用 `url_launcher`（那要给三个平台加插件注册）。
  Windows 上刻意**不走 `cmd /c start`**：我们的安装包名字是中文，
  GitHub 给的链接是百分号转义的（`%E7%BC%96...`），而 cmd 会把 `%...%` 当变量去展开
  —— 万一把链接改坏，用户只会看到一个打不开的页面。`rundll32` 走 CreateProcess，
  不经过命令行解析，`%` 和引号都是普通字符
- **URL 安全校验按「不可信输入」做**：只放行干净的 `https://`；
  先过一遍 `Uri` 规范化（把中文转义成 `%XX`，否则我们自己发的带中文名的安装包链接
  会被自己拒掉），再拒掉 `&` `;` `(` `)` `'` `$` 这些 **Uri 转义不掉、
  会原样进命令行**的字符。被拒就把网址显示出来让用户复制
- **Release 正文是 Markdown**，`cleanReleaseNotes` 去掉标题标记与代码围栏、
  列表符号换成中点，剩下的轻量标记交给项目自己的 `RichMessageText`

## 十·补四、v1.5.1 小结（测试出题范围）✅

四种测试模式（快速/标准/强化/全题库）**各自**可以设定：
从哪些大类出题 + 出到哪个难度档。设置入口在测试页每个模式的下方（📚 控件），
弹窗里选大类、选档位，实时显示「这个范围一共 N 题：简单 x · 中等 y · 困难 z」。

### 难度档位是**累计**的

用户是按颜色理解的，命名就照他的话来，别用 easy/medium/hard：

| 档位 | 颜色 | 含义 |
|------|------|------|
| 难度一 | 绿 | 只出简单 |
| 难度二 | 绿 + 黄 | 简单 + 中等 |
| 难度三 | 绿 + 黄 + 红 | 全部难度 |

用 easy/medium/hard 会让人以为「难度二 = 只出中等」，正好理解反。

**某一档不可选的判据**：范围内得真有一道**恰好那一级**的题。
一门语言 12 个大类里有 5~7 个没有困难题，所以「选了基础语法 → 难度三不可选」
是常事 —— 否则那一档和上一档能出的题一模一样，摆着只会让人以为选了会变难。

### 范围按**序号**存，不按分类 key

三门语言的分类 key 并不一样（python 第 1 个是 `01_syntax`，C/C++ 是 `01_basics`；
C++ 第 11 个是 `11_stl`，另两门是 `11_advanced`），但**序号对应的教学阶段一致**。
所以范围存「第几个大类」，切语言时同一序号落到同一阶段，显示名跟着当前语言走。
代价是一条新的不变量：**三门语言的分类序号必须一一对应**，
所以 `bank_structure.dart` 现在会检查每门语言的序号恰好是 1~12 不重不漏。
（判不了「第 N 个是不是同一阶段」—— 那个只能写在 BANK_SPEC 里靠人守。）

### 抽题：按难度比例分层，而不是洗牌取前 N

直接洗牌取前 N，难度结构是碰运气的：同一份「标准测验」两次做起来可能一次全简单、
一次连出三道困难。改成最大余数法分配名额：

```
名额 = count × 该级题数 / 池子总数   （取整后把余下名额按小数部分发）
```

python 题库是 34 简单 / 32 中等 / 6 困难，抽 10 题 →
`4.72 / 4.44 / 0.83` → 取整 4/4/0（共 8），余下 2 个发给小数最大的困难(.83)和简单(.72)
→ **5 简单 / 4 中等 / 1 困难**。名额是算出来的，所以**换随机种子只换题、不换难度结构**。

最后整体打散一次 —— 不然同一难度的题会连在一起，做起来像换了套卷子。

### 顺手修掉的 bug：测试题池没按语言过滤

`TestPage` 调的是不带参数的 `loadCategories()`，而它返回**全部语言**的题 ——
「全题库」显示的是 216 题（3×72），一次 Python 测验里会混进 C 和 C++ 的题。
多语言重构时 practice_page 跟着改了，这一处漏了。

这个 bug 也是本功能的前提：范围按「第几个大类」选，池子里就必须只有一门语言，
否则序号 1 在池子里同时对应 python 的 `01_syntax`、C 的 `01_basics` 和 C++ 的
`01_basics`（三份完全不同的题）。

## 十·补五、v1.5.x 小结（「我的」重构）🚧 进行中

底栏「设置」改成「我的」：用户找自己数据的地方和找设置的地方本来就是同一处。
设置降为「我的」下的子页面（`SettingsPage` 只被 main.dart 引用过一次，挪起来干净），
「关于」在「我的」里新增入口（设置里那个保留，两个入口指向同一个页面）。

分三段交付：**A 关于页 + 许可证 + 更新日志**（已完成）、
**B「我的」页 + 数据聚合 + 结论**、**C fl_chart 图表 + 活动记录**（均已完成）。

### 数据聚合分两层

`stats_service.dart` 里刻意拆开：

- `StatsService.collect()` 只干 IO（读三门语言的题库、进度、测试历史），
  攒成 `OverallStats` 这个纯数据对象
- `buildConclusions(OverallStats)` 是**纯函数**：只吃数据、吐结论文案

这么拆是为了让「数据不足时该说什么」能被测试覆盖。那种分支没法靠手点出来，
而**说错话的代价比不说大得多** —— 只有 1 次测试却写「正确率在上升」，
用户会觉得这东西在瞎编。所以每条结论都有前置条件，不够时给的是
「再做 2 次就能看趋势」这类实话。规则清单见 `test/stats_service_test.dart`
（20 条，一半在测「什么时候**不该**下结论」）。

### 练习活动记录（C 段新增的数据）

`ProgressService` 原来只存「做没做过」（布尔）和错题次数，**没有时间维度** ——
练习趋势、活跃度、连续天数一个都画不出来。C 段补了一层按天的记录：

```
键：activity_<语言>_<yyyy-MM-dd>   值：当天首次做对的题数
```

三个设计点：

- **只在「未解决 → 已解决」那一次 +1**。学生改一版判一次是常态，
  每判一次都记的话「今天做了 20 题」是假的
- **保留 365 天**，写入时顺手清理。键是每天每语言一个，无限增长不合适
- **导入时同日取较大值**而不是相加：同一个文件导两次不该翻倍，
  两台机器合并时也宁可保守（相加会凭空造出「那天做了 20 题」）

连续天数有个容易做错的地方：**「今天还没做」不算断签**，从昨天起算 ——
否则早上打开应用就看到连续天数归零。

### 测试点必须带时间戳

`TestPoint(at, accuracy)` 而不是只存正确率序列。原因：结论和趋势图都要
「最近 N 次」，而**跨语言合并时只有带上时间才能正确排序**。早先只存序列时，
合并后的「最近一半」可能整段都是另一门语言的 —— 那是静默算错，
看图看不出来，因为图还是画得好好的。

### 图表（fl_chart）

C 段引入 `fl_chart ^1.2.0`（连带 `equatable`）—— 这是本项目除
`shared_preferences` / `path_provider` 外第一个较大的第三方依赖。
五张图：三门语言完成度（条形）、难度分布（环形）、分类完成度（横向条形）、
测试正确率（折线）、近 30 天练习（柱状）。

两条约定：
- **数据不够时不画空图**，给一句占位文案。一条平线会让人以为「我一直在 0」，
  而实际是还没有数据
- **图表不引入新配色**：难度一律用 `difficultyColor`（绿/黄/红），
  其余用主题色。否则「我的」和「关于」两页的视觉语言会打架

### 应用内更新日志是生成的

`assets/CHANGELOG.md` 由 `tools/build_changelog.py` 从 `docs/releases/*.md` 生成。
手抄第二份必然漂移，而漂移是静默的（应用里的日志少一版没人会发现），
所以有防漂移测试盯着，生成器还有 `--check` 模式给发布闸门用。
显示时复用更新弹窗那套 `cleanReleaseNotes`。

## 十一、优化清单（分阶段，待用户挑选）
### P1 体验增强（已完成 ✅）
- [x] 测试默认定位到**下一道未做的题**
- [x] 测试结束时提示**还有 N 题未做**（交卷前确认弹窗）
- [x] 判题结果面板显示**耗时 / 用例通过数**

### P2 功能扩展（已完成 ✅）
- [x] **错题本**：做错的题单独收集 + “未作答”标记区分 + 专项重练
- [x] **计时器**：测试板块倒计时压力模式（时间到自动交卷）
- [x] 题目收藏/标记“待复习” + 收藏复习页
- [x] 进度导出：JSON / CSV 写入文档目录
- 另有：参考代码（题解）、测试历史 + 统计概览卡、逐题回看

### P3 深度体验
- [x] 代码编辑器增强之**语法高亮 + 行号**（自研 `PythonCodeField`，零第三方依赖）
- [x] 自动缩进 / 更多高亮细节
- [x] 更丰富的判题反馈（显示首个用例差异详情）
- [x] 成就/称号系统（连胜、全对勋章）
- [x] 深色/浅色主题自定义强调色

### P4 工程化
- [x] **迁移 Windows**：`flutter build windows` 出 .exe → 捆绑 Python runtime（方案 A）
- [x] **打包安装器**：Windows 用 **Inno Setup**（单文件 `Setup.exe`，免管理员）；
      macOS 用 **dmg**（拖拽安装，卷内附首次打开说明）
- [ ] 数据库替换 shared_preferences（题目量大/需要复杂查询时，可评估 sqlite / drift 或 hive，先查开源）

## 十二、待验证 / 已知缺口

**代码写好但没人真跑过的**（详见 `PROJECT_BACKLOG.md`）：
- [ ] Windows 安装包（Inno Setup 只能在 Windows 上跑，`.iss` 是静态核对过的）
- [ ] Linux 打包（`build_linux.sh` 从未在真 Linux 上跑过）
- [ ] exe 属性页的中文（`llvm-rc` 验证通过，MSVC 的 `rc.exe` 行为可能不同）
- [ ] macOS arm64：universal 包里两份 Python，需要 Apple Silicon 机器确认选对了

**已实机确认**：进度存储、判题（三门语言）、交互终端连续运行、
macOS 打包与签名、Windows 构建与 C/C++ 判题、源码语法要求检查（12 道指针题
的参考答案与「不用指针的等价写法」都在真编译环境下跑过）。

**启发式检查的已知边界**（不是缺陷，是方法的边界）：`pointer.use` 之类靠关键字级
文本分析，认不出小写自定义类型名（`mytype *p` 不会被当成指针声明 ——
宁可漏认也不能把 `a * b` 误认成声明），也挡不住蓄意伪装。
